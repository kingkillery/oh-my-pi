<system-notice>
The user's message above contains the **workflowz** keyword. Use `eval` for a compact multi-agent workflow only when fan-out materially improves coverage, confidence, or scale; otherwise work directly.

Helpers: `agent(...)` runs one subagent, `parallel([...])` runs independent thunks, `pipeline(...)` adds stage barriers, `completion(...)` does stateless scoring/classification, and `phase()`/`log()` update status. Prefer `schema=` when branching on results. If you need longer process guidance, read the relevant docs or skills before expanding the workflow.
</system-notice>
