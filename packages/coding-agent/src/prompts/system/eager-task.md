<system-reminder>
Task delegation is enabled: use `{{toolRefs.task}}` for substantial or parallelizable work after settling the approach.{{#if taskBatch}} Batch independent slices in one parallel call.{{/if}} Work inline only for small edits, direct answers, or explicit self-run commands.
</system-reminder>
